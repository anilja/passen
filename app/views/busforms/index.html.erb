<% @busforms.each do |busform| %>
<div class="container">
  <div class="col-md-8 banner-left">
  <div class="busno_row">
    <h2 class="busform_title"><%= link_to busform.title, busform %></h2>
    <p class="busform_sub_title"><%= busform.description %></p>
  </div>
  </div>
  </div>
<% end %>


<div class="links">
  <%= link_to "New Bus Form", new_busform_path %>
</div>